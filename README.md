1 git checkout develop
2 write code
3 <git add .> + <git commit - m "Commit comment"> = <git commit -am "Commit comment">
  OR <git add . && git commit -m "Commit comment"> if new files was created
5 git push
6 Merge pull request on github
7 <git checkout master && git pull origin master>
8 npm run deploy